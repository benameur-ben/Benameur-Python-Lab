# 🚀 Distributed Asynchronous Web Crawler

[ENG] | [ARB] | [FRA]

---

### [English]
A professional, high-performance asynchronous crawler using Python's `asyncio` and `aiohttp`. It features:
- **Concurrency Control**: Prevents server overload using semaphores.
- **Error Handling**: Robust try-except blocks for network resilience.
- **Speed**: Optimized for large-scale data extraction.

**Usage**:
```bash
pip install aiohttp beautifulsoup4
python async_crawler.py
```

---

### [العربية]
مستكشف ويب متطور يعمل بشكل غير متزامن باستخدام `asyncio` و `aiohttp`. يتميز بـ:
- **التحكم في التزامن**: يمنع الضغط العائد على الخوادم باستخدام تقنية Semaphores.
- **معالجة الأخطاء**: كتل برمجية قوية لضمان استمرارية العمل عند حدوث مشاكل في الشبكة.
- **السرعة**: مصمم لاستخراج البيانات على نطاق واسع بسرعة فائقة.

**طريقة الاستخدام**:
```bash
pip install aiohttp beautifulsoup4
python async_crawler.py
```

---

### [Français]
Un crawler asynchrone haute performance utilisant `asyncio` et `aiohttp`. Caractéristiques :
- **Contrôle de Concurrence**: Utilise des sémaphores pour éviter de surcharger les serveurs.
- **Gestion des Erreurs**: Structure robuste pour une résilience réseau maximale.
- **Performance**: Optimisé pour l'extraction de données à grande échelle.

**Utilisation**:
```bash
pip install aiohttp beautifulsoup4
python async_crawler.py
```
